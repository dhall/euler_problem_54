require "spec_helper"

RSpec.describe EulerProblem54 do
  it "has a version number" do
    expect(EulerProblem54::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
